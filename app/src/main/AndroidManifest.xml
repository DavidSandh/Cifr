<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.mah.ag7406.cifr">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />

    <application
        android:name=".client.SuperClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".client.HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".client.ContactList"
            android:label="Contacts"
            android:parentActivityName=".client.HomeScreen" />
        <activity
            android:name=".client.LoginScreen"
            android:parentActivityName=".client.HomeScreen">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".client.HomeScreen" />
        </activity>
        <activity android:name=".client.ConversationList"
            android:launchMode="standard"/>
        <activity
            android:name=".client.RegistrationScreen"
            android:parentActivityName=".client.HomeScreen">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".client.HomeScreen" />
        </activity>
        <activity android:name=".client.Conversation" />
        <activity android:name=".client.CreateMessage" />
        <activity android:name=".client.SearchActivity"

            android:exported="true">
            android:exported="true"
        </activity>
    </application>

</manifest>