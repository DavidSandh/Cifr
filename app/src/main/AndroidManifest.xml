<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.mah.ag7406.cifr">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".cifrClient.CifrHomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".cifrClient.CifrContactList"
            android:label="Contacts"
            android:parentActivityName=".cifrClient.CifrHomeScreen" />
        <activity
            android:name=".cifrClient.CifrLoginScreen"
            android:parentActivityName=".cifrClient.CifrHomeScreen">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".cifrClient.CifrHomeScreen" />
        </activity>
        <activity android:name=".cifrClient.ConversationList" />
        <activity
            android:name=".cifrClient.CifrRegistrationScreen"
            android:parentActivityName=".cifrClient.CifrHomeScreen">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".cifrClient.CifrHomeScreen" />
        </activity>
        <activity android:name=".cifrClient.Conversation" />
        <activity android:name=".cifrClient.CreateMessage"></activity>
    </application>

</manifest>